const people = ["yoshi", "ryu from streets", "chun-li", "mario"]; // hold on, these are constants? wouldn't it be PEOPLE = [" ",...]
const ages = [20, 25, 30, 35]  // I know constants can't be changed.

console.log(people);

module.exports = {
    people, ages
};